---
kind: pipeline
name: default
type: docker

platform:
  os: linux
  arch: amd64


steps:
- name: nix
  image: nixos/nix
  commands:
  - echo "test"

steps:
  - name: notify
    image: drillster/drone-email
    settings:
      from: drone@pablo.tools
      host: smtp.sendgrid.net
      username: apikey
      password:
        from_secret: email_password
      recipients:
        - test@pablo.tools
