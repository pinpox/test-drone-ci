---
kind: pipeline
name: default
type: docker

platform:
  os: linux
  arch: amd64

steps:
- name: nix
  image: nixos/nix
  commands:
  - echo "test"
  - nix shell 'nixpkgs#hello' -c hello
  - ls -al






