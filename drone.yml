---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: greeting
  commands:
  - pwd
  - eval $(nix shell nixpkgs#openssh -c ssh-agent) 
  - ls
  - whoami
  - nix run 'nixpkgs#hugo'
  - nix shell 'nixpkgs#hello' -c hello
  - echo "hello world"


